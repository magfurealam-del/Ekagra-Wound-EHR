"use client";
import { useState } from "react";
const regions = ["Head / face", "Chest", "Abdomen", "Back", "Upper limb", "Lower limb", "Foot"];
const footZones = ["Individual toes", "Toe web spaces", "Metatarsal heads", "Heel", "Arch", "Midfoot", "Medial border", "Lateral border", "Dorsum", "Ankle"];
export function AnatomySelector({ value, onChange }: { value: string; onChange: (value: string) => void }) {
  const [view, setView] = useState<"anterior" | "posterior">("anterior"); const [foot, setFoot] = useState(false); const [surface, setSurface] = useState<"dorsal" | "plantar">("dorsal");
  return <div className="anatomy-box"><div className="form-section-title"><div><p className="eyebrow">Anatomical location</p><h3>Select one predefined zone</h3></div><span className="badge">Map v1 · clinician review</span></div>{!foot ? <><div className="segmented"><button className={view === "anterior" ? "selected" : ""} onClick={() => setView("anterior")}>Anterior</button><button className={view === "posterior" ? "selected" : ""} onClick={() => setView("posterior")}>Posterior</button></div><div className="body-map"><div className="body-silhouette"><span className="head">●</span><span className="torso">{view}</span><span className="legs">▮ ▮</span></div><div className="zone-list">{regions.map((region) => <button key={region} className={value === `${view} · ${region}` ? "selected" : ""} onClick={() => region === "Foot" ? setFoot(true) : onChange(`${view} · ${region}`)}>{region}</button>)}</div></div></> : <><button className="text-link anatomy-back" onClick={() => setFoot(false)}>← Back to full body</button><div className="segmented"><button className={surface === "dorsal" ? "selected" : ""} onClick={() => setSurface("dorsal")}>Dorsal</button><button className={surface === "plantar" ? "selected" : ""} onClick={() => setSurface("plantar")}>Plantar</button></div><div className="foot-map"><div className="foot-silhouette">{surface}<br /><span>◉</span></div><div className="zone-list">{footZones.map((zone) => <button key={zone} className={value === `Foot · ${surface} · ${zone}` ? "selected" : ""} onClick={() => onChange(`Foot · ${surface} · ${zone}`)}>{zone}</button>)}</div></div></>}<p className="selected-location">Selected: <strong>{value || "No location selected"}</strong></p></div>;
}
