import Link from "next/link"; import { redirect } from "next/navigation"; import { createServerSupabaseClient } from "@/lib/supabase/server";
export default async function PortalCare() { const client = await createServerSupabaseClient(); const { data: { user } } = await client.auth.getUser(); if (!user) redirect("/portal"); return <main className="portal-page"><div className="portal-card"><p className="eyebrow">Patient portal</p><h1>Confirmed care</h1><p>Your portal session is active for {user.phone ?? "your account"}.</p><section className="panel"><h2>Clinical information</h2><p>Only confirmed records will appear here after patient identity is linked to the clinical record.</p><span className="badge">Read-only access</span></section><Link className="text-link" href="/">Return to Ekagra</Link></div></main>; }
